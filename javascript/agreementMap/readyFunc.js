$( document ).ready(function() {
	// listenSlider();



});

function listenSlider(){
	// d3.selectAll('#popGrowthRateSlider').call(d3.slider().axis(d3.svg.axis().orient('bottom').ticks('5')).min(parseInt(1)).max(parseInt(5)).step(1).on("slide", function(evt, value){
	//       	d3.select('#popGrowthRateText').text('Growth Rate: ' + value + '%');
	// }).on('slideend', function(evt, value){
	// 		// growthRate = value;
	// 		// addComparisonMap();
	// 		// addPopMap();
	// 		// addScarcityMap();
	// }));
}